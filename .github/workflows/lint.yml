name: Lint

on:
  push:
    branches: '*'
  pull_request:
    branches: '*'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check with golangci-lint
        uses: actions-contrib/golangci-lint@v1
